import { ToolRegistry } from "../../core/tools/registry"; import { filesystemTools } from "../filesystem/fileTools"; import { searchTools } from "../search/searchTools"; import { diagnosticTools } from "../diagnostics/diagnosticTools"; import { gitTools } from "../git/gitTools"; import { terminalTools } from "../terminal/terminalTool";
export function createDefaultToolRegistry(confirmWriteWithDiff: (relPath:string, content:string)=>Promise<boolean>): ToolRegistry { const r = new ToolRegistry(); [...filesystemTools(confirmWriteWithDiff), ...searchTools(), ...diagnosticTools(), ...gitTools(), ...terminalTools()].forEach(t=>r.register(t)); return r; }
